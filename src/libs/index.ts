export function generateRandomId() {
  return String(Math.floor((Math.random() * .5) * (Math.random() * 100)));
}
export function generateRandomName() {
  return String(Math.floor((Math.random() * .5) * (Math.random() * 100)));
}